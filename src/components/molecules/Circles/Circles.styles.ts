import {StyleSheet} from 'react-native';
export default StyleSheet.create({
  circle: {
    borderRadius: 200,
    borderColor: 'grey',
    borderWidth: 1.5,
    position: 'absolute',
    alignSelf: 'center',
  },
});
