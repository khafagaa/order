export type colors = {
  primary: string;
  white: string;
  txt: string;
  dimmed: string;
  blue: string;
  darkBlue: string;
  dark: string;
};

export type themeType = {
  theme: string;
  changeTheme: () => void;
};
